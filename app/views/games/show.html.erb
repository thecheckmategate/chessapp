<h1>games/show.html || Enrollment</h1><br />

game title: <%= @game.title %><br />
game description: <%= @game.description %><br />
game white_id: <%= @game.white_id %><br />
game black_id: <%= @game.black_id %><br />
game user_id: <%= @game.user_id %><br />
game game_id: <%= @game.id %><br />

	<% if current_user && current_user.enrolled_in?(@game) %> 
		<h2>You are enrolled</h2>
	<% else %> 
		<%= link_to 'Enroll', game_enrollments_path(@game), class: 'btn btn-primary', method: :post %> 
	<% end %>

