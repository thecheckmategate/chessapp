<br />
<!-- <div class="booyah-box col-xs-10 col-xs-offset-1 text-center"> -->
<p><h1><center><%= @game.name %></center></h1></p>
<br />
</div>

<!-- Table version of game board - rest of code in games.scss -->

<table>
<% 8.times do |row_index| %>
  <tr>

    <% 8.times do |col_index|  %>
      <td>
        <% piece = Piece.find_by_position(x, y) %>
        <% if piece.present? %>
          <%= piece.to_ascii_piece %>
        <% end %>
      </td>
    <% end %>
  </tr>
<% end %>
</table>
