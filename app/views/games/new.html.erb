Create Game 

    <%= simple_form_for @game do |f| %>
    <%= f.input :name %>
    <%= f.input :description %>
    <%= f.input :white_id, :as => :hidden, :input_html => { :value => current_user.id } %>
    <%= f.submit 'Create' %>

    <% end %>